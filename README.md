# candidate_transformer
Built a Python pipeline that combines candidate data from multiple sources into one clean profile. It normalizes, merges, and validates information, removes duplicates, tracks data sources, and generates configurable JSON output with confidence scores for reliable hiring decisions.
